import { IconBook2, IconCamera, IconSparkles } from '@tabler/icons-react'
import { useAppStore } from '../store/useAppStore'
import { dailyQuestions } from '../data/pairContent'
export default function ArchivePage(){const memories=useAppStore(s=>s.memories);return <div className="archive-v16"><header className="section-title"><p className="eyebrow">STARLIGHT ARCHIVE</p><h1>우리의 기록</h1><p>질문, 답변, 사진, 표현이 시간이 지나도 사라지지 않아요.</p></header>{memories.length===0?<div className="archive-empty"><IconBook2/><h2>아직 보관된 오늘이 없어요.</h2><p>두 답변이 열린 뒤 마음에 드는 날을 저장해보세요.</p></div>:<div className="memory-grid">{memories.map(m=>{const q=dailyQuestions.find(x=>x.id===m.questionId);return <article key={m.date}><span>{m.date}</span><h2>{q?.question}</h2><p>{m.myAnswer}</p><p>{m.mateAnswer}</p>{m.myPhoto&&<div className="memory-photo"><IconCamera/><img src={m.myPhoto} alt="memory"/></div>}<div className="memory-expression"><IconSparkles/><strong>{m.feedback?.expression}</strong></div></article>})}</div>}</div>}
